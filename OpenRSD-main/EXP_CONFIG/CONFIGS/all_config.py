all_cfgs = dict()
from EXP_CONFIG.CONFIGS.model_ad_config import aligndet_cfgs
from EXP_CONFIG.CONFIGS.model_baselines_config import baselines_cfgs

all_cfgs.update(aligndet_cfgs)
all_cfgs.update(baselines_cfgs)

